% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/auc_roc_curve.R
\name{auc_roc_curve}
\alias{auc_roc_curve}
\title{Create ROC curve and calculate AUC}
\usage{
auc_roc_curve(formula, data, type = c("insample", "cv"), folds = 5)
}
\arguments{
\item{formula}{A logistic regression formula (e.g., \code{y ~ x1 + x2}).}

\item{data}{A data frame containing the response and predictors specified in the formula.}

\item{type}{Character. Either \code{"insample"} (default) to evaluate on the full dataset
or \code{"cv"} to perform cross-validation.}

\item{folds}{Integer. Number of folds to use if \code{type = "cv"} (default 5).}
}
\value{
A numeric value of the AUC. The function also produces a plot of the ROC curve.
}
\description{
This function calculates the ROC curve for a logistic regression model and
computes the Area Under the Curve (AUC). The model is fitted internally using
the \code{fit_logistic()} function. The response can be numeric (0/1), logical,
factor, or character (e.g., "yes"/"no"), and it will automatically be converted
to 0/1. The ROC curve is plotted, and the function returns the numeric AUC value.
}
\details{
The ROC curve and the corresponding Area Under the Curve (AUC) provide a visual
and quantitative assessment of a logistic model's classification performance.
A higher AUC indicates better discrimination between positive and negative classes.
\itemize{
\item \code{type = "insample"} evaluates the model on the full training data.
\item \code{type = "cv"} evaluates the model via cross-validation, combining out-of-sample
predictions from all folds.
}
}
\examples{
\dontrun{
library(ISLR)
data(Default)

# In-sample ROC and AUC
auc_insample <- auc_roc_curve(default ~ balance + income + student,
                              data = Default, type = "insample")
print(auc_insample)

# Cross-validated ROC and AUC
auc_cv <- auc_roc_curve(default ~ balance + income + student,
                        data = Default, type = "cv", folds = 5)
print(auc_cv)
}

}
